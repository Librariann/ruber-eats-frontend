overwrite: true
schema: http://localhost:4000/graphql
documents: "./src/**/*.{tsx,ts}"
generates:
  ./src/__api__/types.ts:
    plugins:
      - typescript
      - typescript-operations
      # - typed-document-node
